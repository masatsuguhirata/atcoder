#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;
using P = pair<int, int>;

int main() {
	int h,w;
	cin >> h >> w;

	vector<string> str;
	rep(i,h) cin >> str(i);

	//隣接するマスに#があればOKを全探索
	

}
